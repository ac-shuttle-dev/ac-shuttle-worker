{
  "name": "resend-webhook-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:edge": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "webhook:test": "node scripts/send-local.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "vitest": "^1.6.0"
  }
}
